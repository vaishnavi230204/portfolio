LIBRARIES THT INSTALLED :-
lucide-react → icon library

react-router-dom → routing for React

tailwind-merge → helps merge Tailwind classes without conflicts

class-variance-authority → utility for managing component class variations

clsx → conditionally join class names

@radix-ui/react-toast → accessible toast (notification) component
